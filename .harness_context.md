# Harness 上下文

## 当前阶段

Sprint 1 Evaluation Complete

## 产品规格

### 产品名称

ZiFiSense 三评委黑盒模拟 Harness

### 核心价值

在不接入 LLM 的前提下，通过真实 REST 和 MCP 网络请求，以可重复、可审计的方式模拟三类比赛评委，并输出机器可读与人工可读的评分证据。

### 评委 Profile

1. 预测性维护评委：验证设备、故障、历史、监测、工况、维修、同线对比、现场补测和审批闭环。
2. IT 评委：验证健康检查、认证授权、OpenAPI 行为、MCP 工具发现、协议真实性、幂等、错误模型和并发。
3. Agent Harness 评委：验证会话隔离、工具白名单、越权防护、人工确认门控、证据版本、挑战码、防重放、审计轨迹和有界执行。

### P0 功能

- 外部黑盒 REST/MCP 客户端，不调用服务内部函数。
- 三个相互隔离的确定性评委场景。
- 检查级评分、硬失败规则和 40/30/30 加权总分。
- Append-only JSONL 证据轨迹，带 SHA-256 哈希链。
- JSON、Markdown、JUnit XML 三种报告。
- Docker Compose 评审环境与单命令入口。
- Pytest 覆盖评分、报告、协议调用和失败语义。

### P1 功能

- MCP Inspector 配置与人工验证说明。
- 固定随机种子、调用步数上限、HTTP 超时和输出脱敏。

### 延后功能

- LLM 评委、Prompt 变体生成、模型打分、LLM 红队测试。
- 真实 EAM/MES/PLC/DCS 集成。

## 技术栈

- Python 3.11 标准库 HTTP 客户端，避免给运行服务增加依赖。
- YAML Profile 与场景定义。
- Docker Compose 运行目标服务和一次性 Harness 容器。
- Pytest 做本地回归。

## 成功标准

- 一条命令可对运行中的服务执行三位评委。
- 所有业务事实来自返回证据，Fixture 必须明确标识为模拟。
- 任一安全硬失败会使整体失败，不允许用平均分掩盖。
- 相同服务状态和种子下，检查集合、分数和判定可重复。

## 已实现

- 三评委确定性黑盒 Runner 和真实 REST/MCP 客户端。
- 40/30/30 评分、80 分门槛和安全硬失败规则。
- 哈希链证据轨迹及 Markdown、JSON、JUnit 报告。
- Docker Compose Harness、MCP Inspector 配置和运行文档。
- 越权控制/Prompt Injection 路由防护与回归测试。

## 评估结果

- 日期：2026-08-29
- Ruff：通过。
- Pytest：58/58 通过。
- 三评委端到端：100/100，通过。
- Critical：0；Major：0。
- 环境限制：当前主机无 Docker CLI，Compose/Profile 已完成 YAML 解析，镜像未在本机实构建。

## 下一步

在具备 Docker 的部署主机运行 Compose 命令进行镜像级复验；LLM 接入后新增独立 LLM Profile，不修改本轮确定性基线。

## API/MCP 全量可用性复验

- 日期：2026-08-30
- 当前阶段：Evaluation Complete
- 三评委 Harness：100/100，通过；26/26 检查通过。
- REST：8/8 唯一路由实际可用。
- MCP：现代发现、兼容初始化和 17/17 Tool 实际调用通过。
- 全量探测：29/29 通过。
- 并发：10/10 MCP 并发调用通过。
- 回归：58/58 Pytest 通过；Ruff 通过。
- Critical：0；Major：1；Minor/验证边界：2。
- Major：`POST /api/v1/events` 的 OpenAPI 成功码为 202，运行时与测试为 200，提交前应统一。
- 状态：通过；正式部署后需补做公网 HTTPS 和目标吞吐复验。

## Sprint 2：LLM 能力接入规划

### 当前阶段

Planning Complete

### 规划时基线（历史记录）

- 规划时代码尚未加入 LLM SDK、Provider、模型或 API Key 配置；Sprint 2A 已完成接入。
- `AgentFacade` 使用规则意图路由、确定性工具编排和模板回复。
- 三个现有 Harness Profile 均为 `llm_enabled: false`。
- 仅配置 `OPENAI_API_KEY` 或其他供应商密钥不会产生模型调用。

### 推荐方案

- 首期使用项目自有 `LLMProvider` 接口和一个官方 SDK Adapter。
- LLM 只负责结构化规划与回答草稿；状态机、工具白名单、补测同意和审批仍由现有服务端代码控制。
- `LLM_ENABLED=false` 为默认值，模型失败时回退到当前确定性模式。
- Pydantic AI 作为多供应商结构化 Agent 备选；LiteLLM 仅在需要集中网关、预算和多云路由时引入。

### 规格文件

- `docs/LLM_INTEGRATION_PLAN.md`

### 下一步

已选择 DeepSeek，确认比赛环境允许访问公网模型 API，并完成 Sprint 2A 实现与验收。

## Sprint 2A：DeepSeek LLM Demo 实现与验收

### 当前阶段

Evaluation Complete

### 已实现

- 将 `/api/v1/events` 的 OpenAPI 成功响应统一为 200。
- 引入 OpenAI Python SDK 2.x，通过 DeepSeek OpenAI-compatible Chat Completions 接口调用模型。
- 默认关闭 LLM；启用时要求 `DEEPSEEK_API_KEY`，配置错误隐藏输入值。
- DeepSeek 只合成安全的只读调查答复，不参与工具执行、任务状态、现场同意或工单审批。
- 模型请求仅包含用户问题、确定性答案、意图、状态、系统候选诊断和脱敏证据摘要。
- JSON Output 经 Pydantic 校验；空响应、无效 JSON、越界证据引用和控制类输出均确定性降级。
- 模型成功/失败通过 `tool_executions` 记录；失败不返回供应商原始异常。

### 评估结果

- 日期：2026-08-30。
- Ruff：通过。
- Pytest：64/64 通过。
- OpenAPI 3.1 合同校验：通过；`POST /api/v1/events` 运行时与静态契约均为 200。
- 三评委端到端 Harness：100/100，通过。
- REST/MCP 全量探测：29/29 通过，其中 8 个业务 REST 路径、双 MCP 协议和 17/17 MCP Tools 均通过。
- DeepSeek Fake Provider：成功、降级、禁用、安全意图跳过、无效 JSON、空响应、虚构引用、控制输出拒绝均通过。
- 真实 DeepSeek 实网 Smoke Test：DeepSeek 与 Agent 均返回 HTTP 200，结构化结果、5 条证据引用和 usage 账本验证通过。
- Critical：0；Major：0。

### 下一步

当前环境已设置 `DEEPSEEK_API_KEY` 和 `LLM_ENABLED=true` 并完成真实模型 Smoke Test；后续 Sprint 2B 再评估受控只读工具规划，不在本轮开放写操作。

## Sprint 2A.1：DeepSeek 每日 10 元硬预算规划

### 当前阶段

Planning Complete，等待用户确认限额口径。

### 产品目标

为本项目发出的 DeepSeek 模型调用设置每日人民币费用硬上限，默认 10 元。达到或无法安全预留剩余额度时不再请求模型，继续返回确定性答案，不能影响 REST、MCP 和任务状态机可用性。

### 方案比较

1. 应用内 SQLite 预算门禁：复用当前数据库和单容器架构，可按请求并发预占、按 DeepSeek 实际 usage 结算；接入和运维成本最低，推荐本 Demo 采用。
2. LiteLLM Proxy：原生支持预算窗口、虚拟 Key 和多模型成本治理，但可靠预算需要独立 Proxy 与 PostgreSQL；对当前单供应商比赛 Demo 过重，适合作为未来多供应商网关。
3. 仅依赖 DeepSeek 控制台余额/用量：适合人工监控，但无法在本应用请求前做原子并发门禁，也无法保证专属于本项目的每日上限，不作为唯一措施。

### P0 功能

- `LLM_DAILY_BUDGET_CNY=10.00`，人民币微分整数记账，不使用浮点金额。
- `LLM_BUDGET_TIMEZONE=Asia/Shanghai`，按北京时间自然日归集。
- 使用独立 DeepSeek API Key，预算只覆盖本项目通过该 Key 发出的调用。
- 请求前按峰值、缓存全未命中和最大输出 Token 计算保守预占成本。
- 输入 Token 上界按系统提示词与请求 JSON 的 UTF-8 字节数估算，避免依赖与 DeepSeek 不一致的第三方 tokenizer。
- SQLite 事务原子更新当日 `spent + reserved`，并发下只有额度充足的调用才能通过。
- 成功后读取 `prompt_cache_hit_tokens`、`prompt_cache_miss_tokens` 和 `completion_tokens`，按配置价格结算并释放预占。
- 超时或无法取得 usage 时，为防止账单漏计，将该次预占按已消费处理。
- 预算不足时不调用 DeepSeek，返回确定性答案，`meta.is_degraded=true`，并记录 `llm_answer_synthesis/SKIPPED/BUDGET_GATE`。
- 预算表跨重启持久化；不依赖进程内计数器。

### 配置

- `LLM_DAILY_BUDGET_CNY=10.00`
- `LLM_BUDGET_TIMEZONE=Asia/Shanghai`
- `LLM_USD_TO_CNY_RATE=7.00`
- `DEEPSEEK_INPUT_CACHE_HIT_USD_PER_MILLION=0.014`
- `DEEPSEEK_INPUT_CACHE_MISS_USD_PER_MILLION=0.44`
- `DEEPSEEK_OUTPUT_USD_PER_MILLION=1.32`

默认采用 `deepseek-v4-flash` 当前峰值价格；汇率和价格均显式配置，供应商调价时不需要改代码。生产环境建议使用略高于当前实际汇率的保守换算值。

### 数据模型

- `llm_daily_budgets`：`budget_date`、`timezone`、`limit_micros_cny`、`spent_micros_cny`、`reserved_micros_cny`、`updated_at`。
- `llm_usage_ledger`：请求 ID、预算日期、Provider、模型、缓存命中/未命中/输出 Token、预占金额、结算金额、状态和时间戳。

### 验收标准

- 余额不足时 OpenAI/DeepSeek 客户端调用次数为 0。
- 并发请求总预占不得超过 10 元，数据库重启后仍保持额度。
- 实际结算使用 DeepSeek usage 分类字段；价格计算采用整数 Decimal/微分，无浮点误差。
- 超时、usage 缺失和进程恢复采用保守记账，不出现 fail-open。
- 跨北京时间零点后使用新预算日；旧账本保留审计。
- `LLM_ENABLED=false` 与现有 64 项测试保持兼容。
- Ruff、完整 Pytest、三评委 Harness 和 API/MCP 全量探测通过。

### 约束说明

应用内门禁只能限制由本项目实例共享数据库并使用该专用 Key 发出的费用。如果同一 Key 被其他应用直接使用，或者多个实例使用互不共享的 SQLite 文件，就不能保证整个 DeepSeek 账户每日不超过 10 元。

### 建议下一步

用户确认采用“北京时间自然日、项目专用 Key、应用内 10 元硬上限”后，交给 Harness Generator 实现 Sprint 2A.1，并由 Harness Evaluator 完成并发与降级验收。

## Sprint 2A.1 实现与验收结果

### 当前阶段

Evaluation Complete

### 已实现

- SQLite 每日预算快照与逐次 usage 账本。
- Decimal/人民币微元整数价格计算，默认 10 元、北京时间和 7.00 汇率。
- 调用前峰值/全未命中/最大输出保守预占，成功后按 DeepSeek usage 分类结算。
- 并发原子条件更新、跨日新预算、跨数据库重开持久化。
- 超时、Provider 异常或 usage 缺失时按预占额核销。
- 预算不足时不调用 Provider，确定性降级并记录 `SKIPPED/BUDGET_GATE`。
- Windows 增加 `tzdata`，确保 `Asia/Shanghai` 与容器行为一致。
- `.env.example` 的开发认证 Hash 可直接用于本地运行；测试显式隔离本地 `.env`。

### 评估结果

- 日期：2026-08-30。
- Ruff：通过。
- Pytest：73/73 通过。
- 三评委 Harness：100/100，通过。
- REST/MCP 全量探测：29/29 通过；17/17 MCP Tools 实际调用通过。
- 并发预算测试：10 个并发预占中仅 5 个成功，`spent + reserved` 精确等于限额，未超领。
- 充值后真实 DeepSeek Smoke Test：会话 HTTP 201、DeepSeek HTTP 200、Agent HTTP 200。
- 成功路径：`meta.is_degraded=false`、LLM Tool 状态为 SUCCEEDED，5 条引用全部通过证据 ID 白名单校验。
- usage 与预算账本：输入缓存未命中 711 Token、输出 991 Token；预占 22,355 微元、实际结算 11,347 微元，悬挂预占为 0，当日限额为 10,000,000 微元。
- 降级路径：另一次 DeepSeek HTTP 200 响应未通过本地结构化校验时，REST 仍返回确定性答案并标记降级，证明故障隔离有效。
- Critical：0；Major：0；外部配置事项：0。

### 外部配置事项

无。账户充值、Key 格式修正和 LLM 开关启用均已完成。

### 决策

Sprint 2A.1 功能与实网验收全部通过。DeepSeek 生成、证据引用、异常降级和每日 10 元预算结算均有真实运行证据。

---

## Sprint 2B：MCP 统一处置引导筛查与改造规格

### 当前阶段

Implementation Complete。设计、实现、部署和 Harness Evaluator 验收均已完成。

### 问题定义

当前 17 个 MCP Tool 的结构化数据、来源声明、模拟标识和写操作门控基本完整，但面向自然语言客户端时容易停留在“查询并复述数据”。服务器 instructions 仅声明模拟数据和禁止生产控制；工具 docstring 只描述单次查询，没有规定跨工具调查顺序。DeepSeek 仅润色确定性答案，输出模型不包含负责人、行动步骤、完成条件、升级条件和下一轮问题。目录型活动故障与评测会话任务未完全打通，导致部分严重故障不能继续进入补测、工单和审批闭环。

### 统一产品目标

用户询问设备、故障、诊断或处置时，系统必须在事实正确的基础上继续提供受证据约束的下一步引导。回答结构统一为：

1. 当前判断：发生了什么、严重度、诊断成熟度和不确定性。
2. 证据依据：确认事实、推断、缺失和冲突分开表达。
3. 处置步骤：按顺序给出动作、原因、执行角色和前置条件。
4. 决策边界：说明哪些条件需要升级人工、现场复核或审批；不编造阈值，不直接控制设备。
5. 下一轮交互：提出最少且阻塞性的具体问题，或明确下一项可调用工具。

底层数据工具继续返回机器可读结构，不在每个 Tool 内复制大段自然语言。所有 Tool 增加统一 `guidance` 结构；`agent_invoke` 负责把结构化事实和 guidance 组织成面向用户的答复。

### 推荐技术方案

采用“官方 MCP SDK 元数据 + 项目内确定性 Guidance Engine + 可选 DeepSeek 表达增强”，不引入新的 Agent 框架。

- 扩充 `MCPServer.instructions`，在前 512 字符内明确调查工作流、证据边界和写操作审批规则。
- 为 17 个 Tool 添加 `title`、面向模型的完整 `description` 和 `ToolAnnotations`。
- 查询工具标注 `read_only_hint=True`、`open_world_hint=False`、`idempotent_hint=True`。
- 状态变更工具按真实语义标注只读、幂等、破坏性提示，服务端安全门控仍是权威，不能依赖客户端 annotations。
- 新增统一 Pydantic Guidance 模型，由服务端确定性生成；LLM 不得新增动作、阈值、证据或审批结论。
- 保持 17 个 Tool 名称和现有输入兼容，避免影响比赛评测器。

### Guidance 数据契约

每个 Tool 的结构化结果增加 `guidance`：

- `summary`：本次结果的具体意义，不使用空泛套话。
- `urgency`：`ROUTINE | PRIORITY | URGENT | CRITICAL`。
- `current_stage`：当前调查或处置阶段。
- `actionability`：`INFORM | INVESTIGATE | DECISION_PENDING | APPROVAL_REQUIRED | COMPLETE`。
- `next_steps[]`：包含 `code`、`title`、`why`、`owner`、`required_inputs`、`requires_consent`、`requires_approval`、`blocking`、可选 `next_tool`。
- `blocking_questions[]`：只包含真正影响下一步判断的问题。
- `escalation_conditions[]`：只引用系统事实或企业既定阈值，不编造数值。
- `constraints[]`：明确模拟数据、证据限制和禁止自动控制。

`agent_invoke` 的确定性响应同步扩展为同一语义结构。DeepSeek 的结构化输出只允许改写 `answer` 和选择已有证据 ID，不允许覆盖 Guidance Engine 的动作、角色、门禁和状态。

### Guidance 架构决策：统一契约，分类策略，状态驱动

17 个 Tool 不共用一段固定的“查完干什么”文案。统一的是 Guidance 数据契约、证据优先级、状态判断顺序、安全门禁和输出质量标准；差异化的是每类 Tool 的指导目标、业务场景和具体下一步。

Guidance Engine 采用三层决策：

1. 基础策略层：模拟数据声明、事实/推断分离、不得编造阈值、不得自动控制、写操作必须服从同意和审批。
2. 工具类型策略层：按导航查询、证据查询、状态变更、审批决策、维修验证和自然语言编排分别生成不同 Guidance。
3. 业务状态策略层：根据严重度、诊断成熟度、数据质量、证据缺口、任务状态、待审批和用户意图生成本次具体步骤。

输入上下文至少包括：`tool_name`、操作类型、用户意图、设备关键度、故障严重度、故障状态、诊断状态、证据质量、缺失字段、冲突、任务状态、现场同意和待审批信息。不得只根据 Tool 名称生成 Guidance。

#### 六类 Guidance Profile

| Profile | Tool | Guidance 目标 |
|---|---|---|
| `INTAKE` | `create_evaluation_session`、`ingest_alarm` | 说明创建了什么调查对象、初始风险和进入调查的下一步 |
| `NAVIGATION` | `list_assets`、`list_current_faults` | 帮助筛选、排序和选择调查对象，只推荐下一项查询，不越级给维修结论 |
| `EVIDENCE` | `get_fault_detail`、`list_fault_history`、`get_monitoring_summary`、`get_operating_context`、`get_maintenance_history`、`compare_peer_assets` | 判断证据支持什么、不支持什么、缺什么，并选择下一项补证动作 |
| `FIELD_EVIDENCE` | `request_field_measurement`、`ingest_field_measurement_result` | 说明同意状态、采集责任、质量门槛和 PASS/PARTIAL/FAIL 分支 |
| `DECISION_TRANSITION` | `draft_work_order`、`decide_work_order_approval` | 说明门控是否满足、影响预览、人工审批和不可重放边界 |
| `VALIDATION_ORCHESTRATION` | `ingest_work_order_completion`、`get_task`、`agent_invoke` | 汇总状态、验证维修效果、决定关闭/重开/继续补证，并形成面向用户的完整行动方案 |

#### 状态驱动示例

- `CRITICAL + ENGINEER_CONFIRMED + ACTION_PENDING`：优先人工复核和检修窗口决策，列出仍需确认的现场条件；不能仅返回“继续观察”。
- `MAJOR + INCONCLUSIVE + 数据质量问题`：先核查传感器、网关和可比工况，再做现场复测；不能建议维修部件。
- `WARNING + CANDIDATE`：补齐工况和趋势，满足升级条件后再进入现场补测；不能夸大为已确认故障。
- `现场结果 PASS`：进入人工工程判断，可评估工单草稿；不自动确认故障。
- `现场结果 PARTIAL/FAIL`：明确重新采集的缺口，保持待补证状态。
- `审批待处理`：只允许批准、拒绝或继续补证，不重新生成 Challenge。
- `维修后改善`：支持验证并关闭；`未改善/证据不足`：保留冲突并重新进入调查。

#### 实现结构

采用一个集中式 `GuidanceEngine`，内部使用类型化 Strategy，不在 17 个 Tool 函数中各自拼接自然语言：

```text
Tool/Agent Result
  -> GuidanceContext（事实与状态）
  -> BaseSafetyPolicy
  -> ProfileStrategy
  -> StateRule
  -> GuidanceEnvelope
  -> 可选 DeepSeek 表达增强
```

`GuidanceEnvelope` 是结构化决策输出；面向用户的自然语言只由 `agent_invoke` 或 MCP Host 根据该结构生成。底层 Tool 的 Guidance 保持短、具体、可机器执行，避免 17 份重复的“AI 建议”。

#### 设计取舍

- 不采用“17 个 Tool 完全统一文案”：会导致查询设备、提交审批和维修验证都给出相同套话。
- 不采用“17 个 Tool 各自独立实现”：会导致字段、门禁和风险口径漂移，难以测试维护。
- 采用“统一引擎 + 六类策略 + 状态规则”：既保持一致性，又能对不同工业场景给出真正不同的下一步。

### 17 个 Tool 筛查矩阵

| Tool | 当前主要问题 | 统一改造要求 |
|---|---|---|
| `create_evaluation_session` | 只支持减速机场景，目录故障不能进入闭环 | 保留场景参数兼容；增加按已有 `fault_id` 建立调查会话的兼容入口，返回下一步 `agent_invoke` 引导 |
| `list_assets` | 只列数据，不解释风险排序和下一步 | 增加活动故障摘要、排序理由和 `list_current_faults` 引导；无故障时说明监测覆盖边界 |
| `list_current_faults` | `next_action_summary` 粒度不一，客户端容易只复述 | 服务端统一风险排序，返回最高优先级理由、应继续读取的详情和下一步工具 |
| `get_fault_detail` | 有问题和动作，但缺少角色、顺序、完成/升级条件 | 输出完整 Guidance；按严重度和证据状态决定补查监测、工况、历史、维修和同类对比 |
| `list_fault_history` | 有相似度但没有说明如何用于本次调查 | 区分已验证、驳回、未定历史；明确只能用于调查优先级，不能替代本次证据 |
| `get_monitoring_summary` | 只返回趋势，缺少后续判断路径 | 根据数据质量和趋势返回工况核对、现场复测或继续观察的条件化步骤 |
| `get_operating_context` | 缺失字段虽明确，但没有转成具体追问 | 只追问缺失且阻塞判断的字段，并说明其影响 |
| `get_maintenance_history` | 维修史容易被客户端误当作因果证据 | 明确关联不等于因果；给出维修前后可比性和需要复核的项目 |
| `compare_peer_assets` | 分析存在但处置价值不清晰 | 按可比性决定是否可用于升级优先级，并明确局限和下一步 |
| `request_field_measurement` | 只返回创建结果 | 明确已创建/重复复用、责任角色、等待内容、结果回传工具和不可升级结论的条件 |
| `ingest_alarm` | 创建任务后缺少调查入口提示 | 返回任务标识、初始优先级和下一步 `agent_invoke/get_task` |
| `ingest_field_measurement_result` | PASS/PARTIAL/FAIL 后续动作未统一 | PASS 进入人工工程判断；PARTIAL/FAIL 返回重新采集要求；不得自动确认故障 |
| `draft_work_order` | 门控失败只说明失败，成功后审批影响不够清晰 | 精确列出缺失门槛；成功时返回影响预览、一次性 Challenge 和下一步审批工具 |
| `decide_work_order_approval` | 只返回决策结果 | 返回不可重放声明、任务新状态、执行边界和后续完成回传要求 |
| `ingest_work_order_completion` | 维修后验证路径表达不足 | 根据改善、未改善和证据不足分别关闭、保留冲突或继续调查 |
| `agent_invoke` | 回答模板固定，动作未按设备/严重度/证据变化；“停机”一律判出域 | 作为统一自然语言出口；允许提供停机决策支持但禁止执行控制，按 Guidance 生成行动方案和最少追问 |
| `get_task` | 返回完整快照但没有告诉用户当前该做什么 | 根据状态、待审批、补测和维修结果生成唯一明确的下一步 |

### 安全和兼容原则

- 保留现有 17 个 Tool 名称、REST 路径、认证、MCP 双协议和结构化字段；新增字段采用向后兼容方式。
- `停机`、`启停`类提问允许进入“决策支持”，但系统只能解释证据、企业阈值和人工审批条件，不能调用生产控制。
- CRITICAL 不等于自动停机；所有升级动作必须引用现有事实、现场 SOP 或明确的人工作用域。
- 无合格现场证据时不得生成工单草稿；无一次性 Challenge 时不得审批。
- 所有目录数据继续标记 `is_simulated=true`；LLM 失败时 Guidance 仍完整可用。
- 10 元每日 DeepSeek 预算门禁保持不变。

### 方案比较

| 方案 | 覆盖度 | 接入成本 | 维护性 | 风险 | 结论 |
|---|---:|---:|---:|---:|---|
| 仅扩充 Prompt、instructions 和 docstring | 中低 | 低 | 中 | 客户端表现不一致、不可稳定验收 | 只作为必要基础，不单独采用 |
| 官方 MCP annotations + 确定性 Guidance Engine + 可选 LLM | 高 | 中 | 高 | 需要统一模型和夹具数据 | 推荐，复用现有 SDK/Pydantic/状态机 |
| 引入 Pydantic AI 或 LangGraph 全自主规划 | 高 | 高 | 中低 | 新依赖、状态重复、审批复杂、比赛不确定性 | 暂不采用，留作多供应商/复杂编排阶段 |

### P0 验收标准

1. `tools/list` 仍精确返回现有 17 个 Tool，全部具有标题、明确用途、下一步说明和正确 annotations。
2. 所有 Tool 结果具有统一 Guidance 语义；空结果、成功、门控失败和异常路径均有具体下一步。
3. 对任一 CRITICAL/MAJOR 活动故障，自然语言答复必须包含优先级理由、至少两个有顺序的下一步、执行角色、证据缺口、升级条件和至少一个阻塞问题。
4. 对 WARNING/INFO 故障不夸大风险；没有事实依据时不建议停机或更换部件。
5. 用户问“是否需要停机”时返回证据化决策支持和人工/SOP 边界，不再简单判为 `OUT_OF_SCOPE`，且不产生控制工具调用。
6. 查询工具在 Codex 中按只读提示运行；写工具仍要求同意、Challenge 或服务端门控。
7. 目录中的 6 个活动故障均可形成可操作调查方案；需要行动的目录故障能进入会话任务闭环。
8. LLM 关闭、失败或预算耗尽时，确定性 Guidance 完整保留。
9. Ruff、全量 Pytest、OpenAPI、REST/MCP 29 项探测、17/17 Tool 调用和三评委 Harness 全部通过。
10. 新增自然语言质量 Profile，覆盖通用套话、无依据动作、越权停机、缺少追问、错误历史类比和写操作绕过。

### 建议 Sprint

- Sprint 2B.1：MCP instructions、Tool annotations、描述和 Guidance 模型。
- Sprint 2B.2：17 个 Tool Guidance 生成、目录故障到任务会话桥接、`agent_invoke` 行动化改造。
- Sprint 2B.3：DeepSeek 结构化表达升级、自然语言质量 Harness、Codex 实际对话验收。

### 下一步

Harness Generator 按 Sprint Contract 实现 Sprint 2B.1～2B.3；完成后由 Harness Evaluator 进行协议、业务、安全、自然语言和实网模型验收。

---

## Sprint 2C：三组账号与独立 API Key

### 当前阶段

Implementation Complete / Evaluation Passed。

### 已实现

- 新增 `API_CLIENTS_JSON` 部署配置，账号由 API Key Hash 自动映射，不改变 Bearer 接入方式。
- 支持每账号独立 Scope、`enabled` 停用、独立 Hash 轮换和重复/非法配置启动校验。
- 保留原两组开发 Hash 作为未配置账号数组时的兼容回退。
- 本地 `.env` 已配置三组账号 Hash，明文未进入受版本控制文件。
- 三组账号均通过 REST、MCP discovery、17 Tool 列表和 `list_assets` 实际调用。

### 验收结果

- Ruff：通过。
- Pytest：87/87 通过。
- REST：三账号 3/3 通过。
- MCP discovery/tools/list/tools/call：三账号 9/9 通过。
- 旧开发 Key：401；review 管理接口：403。
- 受版本控制文件明文 Key 扫描：0 命中。

### 决策

功能通过，可在服务器就绪后直接复用镜像与账号 Hash 配置；届时只需补充公网 URL、外部端口、Host/Origin 和 HTTPS 配置。

---

## Sprint 3：MCP 正式部署兼容性加固

### 当前阶段

Implementation Complete / Local Evaluation Passed。

### 已确认方案

- 使用官方 MCP SDK 注入的请求 `Context` 读取 Authorization，并在 Tool 边界重新解析为可信账号身份。
- 同步 Tool 继续采用确定性降级，不增加 MCP Tasks；生产 LLM 禁用自动重试并受 25 秒同步预算约束。
- 使用独立 `compose.production.yaml` 和 Caddy 自动 HTTPS，保留现有本地 Compose。
- 生产 MCP 限流配置为 6000 次/分钟，并增加官方 SDK Smoke Test 和单连接并发负载探测。

### 规格与验收

见 `sprint_contract.md` 的“Sprint 3：MCP 正式部署兼容性加固”。

### 实现与验收结果

- 9 个带会话或写语义的 MCP Tool 已使用请求 Bearer 对应的真实 `client_id`；双账号相同幂等键及跨账号任务读取隔离测试通过。
- 同步预算固定为 25 秒；LLM 默认 12 秒、0 次 SDK 自动重试，保留至少 5 秒确定性降级窗口。
- 新增 `compose.production.yaml`、`Caddyfile`、`.env.production.example` 和 `docs/PRODUCTION_DEPLOYMENT.md`；应用 8080 不对宿主机暴露。
- 官方 MCP SDK 协商 `2026-07-28`，发现 17 个 Tool，真实 `list_assets` 调用成功且不泄露 Token。
- Pytest 92/92、Ruff、REST/MCP 29/29、三评委 100/100 全部通过。
- 生产限流 6000 次/分钟下，本机 20 并发、10 连接、200 次调用：200/200 成功，110.11 QPS，P95 387.56 ms。
- 当前机器没有 Docker CLI；镜像构建、Caddy 自动证书和公网 HTTPS 复验是目标服务器部署门禁，不计为本地虚假通过。

### 决策

代码与部署资产可进入正式服务器部署。正式放行仍要求目标服务器执行 Compose 配置解析、镜像构建、HTTPS Smoke Test 和同规格负载探测。
